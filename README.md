# neural_net_from_scratch
Neural network built completely from scratch

Includes an input and output layer with 2 hidden layers. The number of neurons in each layer is variable.
Uses sigmoid function for binary classification, outputs range from 0.0 to 1.0
